#!/bin/bash

{{ template "utils" . }}

if exist brew; then
    eval "$(brew shellenv)"
    success "brew found. running package install."
{{ template "brew" . }}
else
    info "brew command not found in PATH. skipping brew packages."
fi
